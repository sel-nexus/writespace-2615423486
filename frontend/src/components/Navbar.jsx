import { useState } from 'react';
import { Link, NavLink, useNavigate } from 'react-router-dom';
import Avatar from './Avatar.jsx';
import { clearSession, getSession } from '../utils/auth.js';
/** Render authenticated responsive navigation and session exit control. */
export default function Navbar() { const session = getSession(); const [open, setOpen] = useState(false); const navigate = useNavigate(); const links = [{ to: '/blogs', label: 'All Blogs' }, { to: '/write', label: 'Write' }, ...(session?.role === 'admin' ? [{ to: '/admin', label: 'Dashboard' }, { to: '/users', label: 'Users' }] : [])]; const logout = () => { clearSession(); navigate('/'); }; return <header className="sticky top-0 z-20 border-b border-slate-100 bg-white shadow-sm"><nav className="mx-auto max-w-6xl px-4 py-3"><div className="flex items-center justify-between"><Link className="font-bold text-indigo-600" to="/">WriteSpace</Link><button className="rounded p-2 text-slate-700 md:hidden" aria-label="Toggle navigation" onClick={() => setOpen(!open)}>☰</button><div className="hidden items-center gap-2 md:flex">{links.map((link) => <NavLink key={link.to} to={link.to} className={({ isActive }) => `${isActive ? 'rounded-full bg-indigo-600 text-white' : 'text-slate-600'} px-3 py-2 text-sm font-medium`}>{link.label}</NavLink>)}<Avatar role={session.role} small /><button className="text-sm text-slate-600" onClick={logout}>Logout</button></div></div>{open && <div className="mt-3 grid gap-2 md:hidden">{links.map((link) => <NavLink key={link.to} onClick={() => setOpen(false)} to={link.to} className="rounded px-3 py-2 text-sm font-medium text-slate-700">{link.label}</NavLink>)}<button className="px-3 py-2 text-left text-sm text-slate-700" onClick={logout}>Logout</button></div>}</nav></header>; }