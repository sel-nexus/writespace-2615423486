import { useState } from 'react';
import { Link, Navigate, useNavigate } from 'react-router-dom';
import { getSession, setSession } from '../utils/auth.js';
import { getUsers, saveUsers } from '../utils/storage.js';
import { usernameAvailable } from '../utils/format.js';
/** Register a local writer account and immediately create its session. */
export default function RegisterPage() { const session = getSession(); const navigate = useNavigate(); const [values, setValues] = useState({ displayName: '', username: '', password: '', confirm: '' }); const [error, setError] = useState(''); if (session) return <Navigate to={session.role === 'admin' ? '/admin' : '/blogs'} replace />; const submit = (event) => { event.preventDefault(); if (values.password !== values.confirm) return setError('Passwords do not match.'); const users = getUsers(); if (!usernameAvailable(values.username, users)) return setError('Username is already taken.'); const user = { id: crypto.randomUUID(), displayName: values.displayName.trim(), username: values.username.trim(), password: values.password, role: 'user', createdAt: new Date().toISOString() }; saveUsers([...users, user]); setSession({ userId: user.id, username: user.username, displayName: user.displayName, role: user.role }); navigate('/blogs'); }; return <main className="min-h-[100dvh] bg-gradient-to-br from-indigo-600 via-violet-600 to-pink-500 p-4"><form onSubmit={submit} className="mx-auto mt-16 max-w-md rounded-xl bg-white p-8 shadow-xl"><h1 className="text-3xl font-bold text-indigo-700">Join WriteSpace</h1>{error && <p role="alert" className="mt-4 text-red-600">{error}</p>}{[['Display Name','displayName','text'],['Username','username','text'],['Password','password','password'],['Confirm Password','confirm','password']].map(([label,key,type]) => <label key={key} className="mt-4 block">{label}<input aria-label={label} type={type} required value={values[key]} onChange={(e) => setValues({ ...values, [key]: e.target.value })} className="mt-1 w-full rounded border p-2" /></label>)}<button className="mt-6 w-full rounded-lg bg-indigo-600 px-4 py-2 font-medium text-white" type="submit">Create account</button><p className="mt-4 text-sm">Already registered? <Link className="text-indigo-600" to="/login">Login</Link></p></form></main>; }