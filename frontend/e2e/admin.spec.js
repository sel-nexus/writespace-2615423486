import { test, expect } from '@playwright/test';
test('administrator manages local accounts', async ({ page }) => { await page.goto('/login'); await page.getByLabel('Username').fill('admin'); await page.getByLabel('Password').fill('admin'); await page.getByRole('button', { name: 'Login' }).click(); await expect(page.getByRole('heading', { name: 'Administrator dashboard' })).toBeVisible(); await page.getByRole('link', { name: 'Manage Users' }).click(); await page.getByLabel('Display Name').fill('Eli'); await page.getByLabel('Username').fill('eli'); await page.getByLabel('Password').fill('pass'); await page.getByRole('button', { name: 'Create User' }).click(); await expect(page.getByText('Eli')).toBeVisible(); await expect(page.getByTitle('Default admin cannot be deleted.')).toBeDisabled(); });